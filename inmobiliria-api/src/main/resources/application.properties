# Base de datos - Railway (conexión pública para desarrollo local)
spring.datasource.url=jdbc:postgresql://switchyard.proxy.rlwy.net:40450/railway
spring.datasource.username=postgres
spring.datasource.password=llSKbeeCiraKXMfSInEjAXGZtiidwpFI
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Servidor
server.port=8080

# JWT
jwt.secret=inmobiliariaSecretKey2025SuperSegura
jwt.expiration=86400000

logging.level.org.springframework.security=DEBUG
